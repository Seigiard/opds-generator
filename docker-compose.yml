services:
  opds:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./files:/books:ro
      - opds-data:/data
    environment:
      - FILES=/books
      - DATA=/data
      - PORT=8080
      - BASE_URL=http://localhost:8080
    restart: unless-stopped

volumes:
  opds-data:
